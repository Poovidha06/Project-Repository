PORT=5000
JWT_SECRET=change_this_to_a_long_random_secret
JWT_EXPIRES_IN=7d
CORS_ORIGIN=http://localhost:5173

# When you're ready to move off the built-in JSON file database,
# plug in MongoDB Atlas here and swap utils/db.js for a Mongoose connection.
# MONGO_URI=mongodb+srv://<user>:<password>@cluster.mongodb.net/lostfound

# When you're ready to move image storage to the cloud:
# CLOUDINARY_CLOUD_NAME=
# CLOUDINARY_API_KEY=
# CLOUDINARY_API_SECRET=
